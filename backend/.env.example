PORT=5000
HOST=0.0.0.0
NODE_ENV=production
# Optional: Provide OpenAI API Key (OPENAI_API_KEY=sk-...). If empty, the engine uses the deterministic Fallback Layout Engine.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
