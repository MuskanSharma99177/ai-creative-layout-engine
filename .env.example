# Backend Environment Configuration
PORT=5000
HOST=0.0.0.0
NODE_ENV=production
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Frontend Environment Configuration (Optional)
# Set in production if frontend is hosted on a separate domain/CDN from the backend
VITE_API_BASE_URL=
