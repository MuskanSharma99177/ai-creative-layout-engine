# Frontend Environment Configuration
# If backend is deployed to a separate URL (e.g. on Render), specify it here:
# Example: VITE_API_BASE_URL=https://your-backend-service.onrender.com
# Leave empty if frontend is served directly by the backend Express server
VITE_API_BASE_URL= https://ai-creative-layout-engine-3.onrender.com
